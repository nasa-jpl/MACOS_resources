# Generated artifacts.  Regenerate:
#   matlab -batch "run_pupil_id('both'); exit(0)"   (or run('pupil_id.m'))
# The driver consumes ../<case>/<case>.in and writes no deck of its own, so
# (unlike the design drivers) there is no *.in deliverable to track.  The
# runner collects every artifact under results/<case>/ (revised Rx included).
*.png
*.mat
results/
__pycache__/
