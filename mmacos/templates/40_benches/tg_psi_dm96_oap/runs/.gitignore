# tg96-oap run bulk (regenerated by tg96_run) -- commit the pruned *_results.mat
# + <tag>_report.txt + <tag>_{test,ref}.in + figures, not these:
*/tg96_flat.txt
*/fort.6
*/*.mat
!*/*_results.mat
*_tail.mat
*/*_layout.png
*/*_transfer.png
# Bench.sketch's per-arm optics outlines: regenerated by every `figs` run and
# shown by no report (the deck uses the vlayout render).  Un-ignore one if a
# report ever quotes it.
*/*_sketch.png
*/*_sketch_ref.png
# the fold/loss/conj probes' scratch grid + their per-angle decks (regenerable;
# the committed record is the <tag>_*.txt table and its figure)
*/fold_flat.txt
*/loss_flat.txt
*/loss_a[0-9]*.in
