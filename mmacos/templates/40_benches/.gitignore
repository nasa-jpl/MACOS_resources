# per-color decks regenerated by the *_s6color.m stages (only Wavelen= differs)
*_[0-9]*nm.in
# the legacy (asymmetric-sandwich) deck zwfs_s7iter.m re-emits for the model-correction record
zwfs_test_legacy.in
