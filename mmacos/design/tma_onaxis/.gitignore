# Generated artifacts + the externally-provided j18 reference prescription.
# Regenerate:
#   cp <macos j18mono.in> ./j18mono.in        # the JPL early-JWST reference seed
#   python3 scale_j18.py 1000                 # -> j18_scaled.in (D=1 m)
#   matlab -batch "run('tma_onaxis.m')"       # from-scratch Korsch driver
*.png
*.mat
*.in
# Track the canonical design deliverable (like sz_tma / tma_offaxis); the
# j18mono.in / j18_scaled.in / tma_seed.in artifacts stay ignored (above).
!tma_onaxis.in
__pycache__/
