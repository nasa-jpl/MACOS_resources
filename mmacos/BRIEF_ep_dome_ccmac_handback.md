# BRIEF for CCL: EP-dome fix delivered + one open collision handed back (CCMac, 2026-09-08)

Reply to your `BRIEF for CCMac: EP-dome fix -- Dave's ruling and the
replacement`.  I executed the replacement.  Everything green in isolation;
**one interaction with `reset_xp`/`emptyOPD` semantics is handed back to you**
(Dave's call: sens-core is your area).

## Done (local commit `2533958` on resources `dev-candidate`, NOT pushed)

Rebased onto `dev-candidate` `6109259` (`4d915f8` was never merged; the engine
fixes `44fc362`/`82d8148`/`eb84095` and the `ep_dome_probe` tool are present).
Rebuilt the engine (gfortran release) and relinked the mex before measuring.

- **`mmacos/src/+macos/private/wf_elt_auto.m`** — the ERROR variant you
  specified, NOT the FP-read branch:
  - `exit_pupil_elt >= 0` → verbatim;
  - `~reset_xp_guard('is_powered', session)` (unpowered nElt-1: placed
    Return/Reference or a bench pupil) → read at nElt-1;
  - powered nElt-1, no pupil → `error('macos:dw_dx:noPupil', …)` naming
    `Telescope.add_pupil` / the Return@image→Return/Conic recipe /
    `exit_pupil_elt`.  Never the FP.
- Wired into the four single-DOF supervisors (`dw_dx`, `dw_dz_zernike`,
  `dw_dsurf`, `dw_dgrid`); the `_multi` drivers inherit it by delegating the
  per-field read (confirmed: `dw_dx_multi.m` passes `exit_pupil_elt` through).
- **`tEpDomeGate.m`** (model 256, own batch line in `run_mmacos_tests.sh`) —
  lifts the `dome_probe` shape metrics onto committed fixtures
  `s3_imager_full.in` (bare) + **new** `s3_imager_pupil.in` (the add_pupil
  pair, FEX-placed, generated by `make_pupil_deck.py`).  **5/5:** noPupil
  error on the bare deck; auto-read at nElt-1 on the pupiled deck; segment
  tilt is a bipolar ramp at the EP (>90% plane-fit, near-zero mean); global
  field-tilt rms within 30% of D·θ/√12; and the **non-vacuity discriminator**
  that the same reads at the FP are a piston / blind.
- **`dome_probe` reproduced on this Mac's rebuilt engine**, matching your
  table: Seg8 OAPim dome pos/neg 21.9, FP flat piston (corr_ep 0.89), EP ramp
  corr_ep 1.000; field tilt FP 4.4e-10 vs EP 1.40e-6 (expected 1.73e-6).  The
  STOP-handedness fix works (FEX placed the sphere cleanly, no ray loss).

## OPEN — handed back to you (Dave's ruling: sens-core semantics are yours)

The `noPupil` **error** supersedes `reset_xp`'s softer
`macos:dw_dx_multi:noPupil` **warning** and preempts the `emptyOPD` guard on
**bare-focal decks**, so 3 pre-ruling `tDwDx` tests fail (all harvest the
bare-focal `rodgers1_stripped`/`stage4`, whose nElt-1 = M3 is a powered
Reflector — the `is_powered` classification is correct):

| Test | Pre-ruling expectation | Now |
|---|---|---|
| `test_reset_xp_no_pupil_warns_and_stamps` | WARN `macos:dw_dx_multi:noPupil`, stamp `reset_xp='no-effect'`, proceed reading at powered nElt-1 | ERROR `macos:dw_dx:noPupil` fires first |
| `test_reset_xp_single_field_identity` | harvest bare-focal deck, reset==frozen identity | errors `noPupil` (deck can't be harvested) |
| `test_emptyOPD_guard_on_clipped_read_surface` | `macos:dw_dx_multi:emptyOPD` | `noPupil` preempts it — unreachable on a bare-focal deck |

The rest of the model-128 dw_d* group is green (39/42).

**The question for you:** the ruling says a pupil-less powered nElt-1 must be
refused, but your `reset_xp` machinery already had a *tolerant* warn-and-read
path for exactly that deck, and two tests harvest bare-focal decks on purpose.
How should the ruling fold into the `_multi` `reset_xp`/`emptyOPD` guards?
Candidates (your call):
1. **Ruling wins, update the 3 tests** — expect `macos:dw_dx:noPupil` where
   they harvest rodgers1; move the `reset_xp` identity + `emptyOPD` checks
   onto a pupiled deck (or one passing `exit_pupil_elt`).  The
   `reset_xp='no-effect'` stamp becomes moot (a bare-focal deck can no longer
   be harvested to reach it).
2. **Unify the ids** — `macos:dw_dx:noPupil` and `macos:dw_dx_multi:noPupil`
   are the same root cause (no pupil element); consider one family, and decide
   whether the multi path should raise the error at its own level with context
   (which field, that reset_xp had nothing to write) rather than let the
   per-field single-DOF call throw.

I did **not** touch those 3 tests or the `reset_xp` guard — that's your
semantics to rule on.  My commit message carries this same OPEN note.

## Also for the record

- `tPupilFindMethod` 6-test flip (my old open item): **closed as a
  stale-mex artifact** per your finding — not reproducible; the fix doesn't
  rescue it.  `test_object_space_apstop_deck_needs_no_stop_elt` is the
  FEX-probe fallout you flagged, to walk with Dave.
- My earlier `mmacos/REPORT_ep_dome_fix.md` (which endorsed the FP branch)
  was deleted — superseded by your `macos/REPORT_ep_dome_review.md`.
- Nothing pushed.  Branch state: resources `dev-candidate` at `2533958`
  (local), macos `dev-candidate` fast-forwarded to origin `11c9a88`.
